# Arduino Examples Moved

First-party Arduino sketches now live in `../arduino/`.

Bundled libraries now live in `../arduino/libraries/`.

This directory is kept only as a compatibility note for older links that referenced `examples/Arduino-v3.3.5/`.
