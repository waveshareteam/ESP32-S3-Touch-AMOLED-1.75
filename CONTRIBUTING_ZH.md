# 参与贡献

[English](CONTRIBUTING.md)

感谢你帮助改进本仓库。

## 工作流程

1. 对于根因尚不明确的行为缺陷或兼容性问题，请先提交 Issue。
2. 每次修改聚焦一个主题。
3. 路径、支持版本或用户操作流程发生变化时，同步更新文档。
4. ESP-IDF 和 Arduino 示例的源码构建由 CI 验证。

## 示例与固件

- 第一方 ESP-IDF 工程直接位于 `examples/esp-idf/` 下。
- 第一方 Arduino 示例直接位于 `examples/arduino/` 下。
- 内置库中的示例属于依赖或上游样例，不是产品 CI 的构建目标。
- `firmware/brookesia/` 是独立维护的源码工程，不属于示例自动发现范围。
- 替换工厂或恢复固件时，必须同时提供对应的构建来源、校验值、交付文档和实机验证依据。

## 文档

英文和中文入口文档必须保持同步。用户可见的硬件规格、命令、链接、固件文件名、校验值或
支持说明发生变化时，应在同一个 Pull Request 中更新对应语言的文档。

## Pull Request

Pull Request 应包含：

- 简明的修改摘要。
- 受影响的示例、文档、固件或工作流路径。
- 已执行的 CI 或其他验证。
- 已执行的硬件验证；如不适用，请明确说明原因。
- 对固件或 SD 卡产物的影响；如适用，请更新校验值。
- 已知限制和后续工作。

公开仓库文本、日志、提交或产物中不得包含本机绝对路径、用户名、工具安装目录、Wi-Fi 凭据、
Token、私钥、私人对话或媒体内容，以及其他主机专属或敏感信息。只提交评审所需的最少诊断
内容，并在发布前完成脱敏。
