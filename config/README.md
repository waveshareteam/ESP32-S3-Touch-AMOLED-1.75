# Shared Configuration

This directory is reserved for shared ESP-IDF configuration overlays and CI configuration inputs.

Project-local `sdkconfig.defaults` files remain inside each example until a setting is shared by multiple projects and should be promoted here.
